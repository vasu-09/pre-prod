# Copy this file to .env and adjust the URL so that phones on your network can reach the API gateway.
# The Expo CLI reads these values before bundling and exposes them to the app via expo-constants.
EXPO_PUBLIC_API_URL=https://api-preprod.mocconnect.in
# Uncomment to use a non-default gateway port
# EXPO_PUBLIC_API_PORT=8080